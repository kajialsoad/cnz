# হাইব্রিড স্টোরেজ সিস্টেম - ব্যবহার গাইড

## সারসংক্ষেপ

আপনার অ্যাপ্লিকেশনে এখন **দুই ধরনের স্টোরেজ সিস্টেম** আছে:

1. **লোকাল স্টোরেজ** (ডিফল্ট) - সার্ভারে ফাইল সেভ হয়
2. **ক্লাউড স্টোরেজ** (অপশনাল) - Cloudinary তে ফাইল সেভ হয়

## কিভাবে কাজ করে?

### লোকাল স্টোরেজ মোড (বর্তমান - ডিফল্ট)

**সেটিং:** `server/.env` ফাইলে `USE_CLOUDINARY=false`

```
ইউজার ছবি আপলোড করে
    ↓
সার্ভারের uploads/ ফোল্ডারে সেভ হয়
    ↓
ডাটাবেসে লোকাল URL সেভ হয়
    ↓
মোবাইল অ্যাপ ও অ্যাডমিন প্যানেল লোকাল সার্ভার থেকে ছবি দেখায়
```

**সুবিধা:**
- ✅ ইন্টারনেট লাগে না
- ✅ কোনো খরচ নেই
- ✅ সম্পূর্ণ কন্ট্রোল
- ✅ দ্রুত কাজ করে

### ক্লাউড স্টোরেজ মোড (অপশনাল)

**সেটিং:** `server/.env` ফাইলে `USE_CLOUDINARY=true`

```
ইউজার ছবি আপলোড করে
    ↓
Cloudinary তে আপলোড হয়
    ↓
ডাটাবেসে Cloudinary URL সেভ হয়
    ↓
মোবাইল অ্যাপ ও অ্যাডমিন প্যানেল Cloudinary CDN থেকে ছবি দেখায়
```

**সুবিধা:**
- ✅ সার্ভারে জায়গা লাগে না
- ✅ দ্রুত লোড হয় (CDN)
- ✅ অটোমেটিক ইমেজ অপটিমাইজেশন
- ✅ স্কেলেবল

## কিভাবে সেটআপ করবেন?

### লোকাল স্টোরেজ ব্যবহার করতে (ডিফল্ট)

1. `server/.env` ফাইল খুলুন
2. নিশ্চিত করুন: `USE_CLOUDINARY=false`
3. সার্ভার রিস্টার্ট করুন
4. ✅ কাজ শুরু!

**কোনো অতিরিক্ত সেটআপ লাগবে না!**

### ক্লাউড স্টোরেজ এনাবল করতে

1. Cloudinary তে একাউন্ট তৈরি করুন (https://cloudinary.com)
2. Dashboard থেকে credentials কপি করুন
3. `server/.env` ফাইলে যোগ করুন:
```bash
USE_CLOUDINARY=true
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret
```
4. সার্ভার রিস্টার্ট করুন
5. ✅ এখন সব ছবি Cloudinary তে যাবে!

## কখন কোনটা ব্যবহার করবেন?

### লোকাল স্টোরেজ ব্যবহার করুন যদি:
- ✅ ডেভেলপমেন্ট/টেস্টিং করছেন
- ✅ ছোট প্রজেক্ট (কম ইউজার)
- ✅ ক্লাউড খরচ বাঁচাতে চান
- ✅ সম্পূর্ণ কন্ট্রোল চান

### ক্লাউড স্টোরেজ ব্যবহার করুন যদি:
- ✅ প্রোডাকশনে যাচ্ছেন
- ✅ অনেক ইউজার আছে
- ✅ দ্রুত লোডিং চান
- ✅ সার্ভার স্পেস সীমিত

## স্টোরেজ মোড পরিবর্তন করা

### লোকাল থেকে ক্লাউডে যেতে:

1. `.env` ফাইলে `USE_CLOUDINARY=true` করুন
2. Cloudinary credentials যোগ করুন
3. সার্ভার রিস্টার্ট করুন
4. ✅ নতুন আপলোড Cloudinary তে যাবে
5. পুরনো ফাইল লোকালে থাকবে (কাজ করবে)

### ক্লাউড থেকে লোকালে ফিরতে:

1. `.env` ফাইলে `USE_CLOUDINARY=false` করুন
2. সার্ভার রিস্টার্ট করুন
3. ✅ নতুন আপলোড লোকালে যাবে
4. পুরনো Cloudinary ফাইল কাজ করবে

## ফাইল URL ফরম্যাট

### লোকাল স্টোরেজ URL:
```
http://localhost:4000/api/uploads/image/1234567890_abc123.jpg
http://localhost:4000/api/uploads/voice/1234567890_def456.mp3
```

### Cloudinary URL:
```
https://res.cloudinary.com/your-cloud/image/upload/v123/clean-care/complaints/images/abc123.jpg
https://res.cloudinary.com/your-cloud/video/upload/v123/clean-care/complaints/voice/def456.mp3
```

## মোবাইল অ্যাপ ও অ্যাডমিন প্যানেল

### কোনো পরিবর্তন লাগবে না!

- ✅ মোবাইল অ্যাপ অটোমেটিক দুই ধরনের URL সাপোর্ট করে
- ✅ অ্যাডমিন প্যানেল অটোমেটিক দুই ধরনের URL সাপোর্ট করে
- ✅ ব্যাকএন্ড সঠিক URL রিটার্ন করে
- ✅ ইউজার কোনো পার্থক্য বুঝবে না

## সমস্যা সমাধান

### ছবি লোড হচ্ছে না?

**লোকাল স্টোরেজ মোডে:**
1. চেক করুন `uploads/` ফোল্ডার আছে কিনা
2. চেক করুন সার্ভার চলছে কিনা
3. চেক করুন URL সঠিক কিনা

**ক্লাউড স্টোরেজ মোডে:**
1. চেক করুন Cloudinary credentials সঠিক কিনা
2. চেক করুন ইন্টারনেট কানেকশন আছে কিনা
3. Cloudinary dashboard চেক করুন

### Cloudinary কাজ করছে না?

1. `.env` ফাইলে `USE_CLOUDINARY=false` করুন
2. সার্ভার রিস্টার্ট করুন
3. ✅ লোকাল স্টোরেজে ফিরে যাবে
4. সব কিছু আগের মতো কাজ করবে

## খরচ

### লোকাল স্টোরেজ:
- 💰 **বিনামূল্যে**
- শুধু সার্ভার স্পেস লাগে

### Cloudinary:
- 💰 **ফ্রি টায়ার:** 25GB storage, 25GB bandwidth/month
- 💰 **পেইড প্ল্যান:** বেশি ব্যবহারের জন্য

## সুপারিশ

### ডেভেলপমেন্ট:
```bash
USE_CLOUDINARY=false  # লোকাল ব্যবহার করুন
```

### প্রোডাকশন (ছোট):
```bash
USE_CLOUDINARY=false  # লোকাল যথেষ্ট
```

### প্রোডাকশন (বড়):
```bash
USE_CLOUDINARY=true   # ক্লাউড ব্যবহার করুন
```

## সারসংক্ষেপ

✅ **ডিফল্ট:** লোকাল স্টোরেজ (কোনো সেটআপ লাগবে না)
✅ **অপশনাল:** ক্লাউড স্টোরেজ (যখন দরকার তখন এনাবল করুন)
✅ **ফ্লেক্সিবল:** যেকোনো সময় পরিবর্তন করতে পারবেন
✅ **সেফ:** পুরনো ফাইল কাজ করবে
✅ **সহজ:** শুধু environment variable পরিবর্তন করুন

---

**প্রশ্ন থাকলে বা সাহায্য লাগলে জানান!** 🚀
