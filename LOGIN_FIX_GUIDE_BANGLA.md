# লগইন সমস্যা সমাধান গাইড

## ✅ পাসওয়ার্ড নিশ্চিত করা হয়েছে

আমরা নিশ্চিত করেছি যে পাসওয়ার্ড সঠিক:

**Email:** `superadmin@demo.com`  
**Password:** `Demo123@#`

ডাটাবেসে পাসওয়ার্ড ম্যাচ হচ্ছে ✅

---

## 🔧 সমস্যা সমাধানের ধাপ

### ধাপ ১: সার্ভার চালু আছে কিনা চেক করুন

**টার্মিনাল ১ (ব্যাকএন্ড):**
```bash
cd server
npm run dev
```

আপনার দেখা উচিত:
```
Server listening on:
  - Local:   http://localhost:4000
```

### ধাপ ২: অ্যাডমিন প্যানেল চালু করুন

**টার্মিনাল ২ (ফ্রন্টএন্ড):**
```bash
cd clean-care-admin
npm run dev
```

আপনার দেখা উচিত:
```
  ➜  Local:   http://localhost:5173/
```

### ধাপ ৩: সঠিক URL ব্যবহার করুন

ব্রাউজারে যান:
```
http://localhost:5173/admin/login
```

**নোট:** `http://localhost:5173` নয়, পুরো পাথ `http://localhost:5173/admin/login` ব্যবহার করুন

### ধাপ ৪: লগইন করুন

1. Email: `superadmin@demo.com`
2. Password: `Demo123@#` (ঠিক এভাবে টাইপ করুন)
3. "Sign in to Dashboard" বাটনে ক্লিক করুন

---

## 🐛 যদি এখনও কাজ না করে

### ব্রাউজার কনসোল চেক করুন

1. ব্রাউজারে F12 চাপুন
2. "Console" ট্যাবে ক্লিক করুন
3. লগইন বাটনে ক্লিক করুন
4. কোনো লাল রঙের এরর দেখা যাচ্ছে কিনা দেখুন

### সাধারণ সমস্যা এবং সমাধান

#### সমস্যা ১: "Network Error" বা "Failed to fetch"

**কারণ:** ব্যাকএন্ড সার্ভার চালু নেই

**সমাধান:**
```bash
cd server
npm run dev
```

#### সমস্যা ২: "Invalid credentials"

**কারণ:** পাসওয়ার্ড ভুল টাইপ হয়েছে

**সমাধান:**
- পাসওয়ার্ড ফিল্ড ক্লিয়ার করুন
- ঠিক এভাবে টাইপ করুন: `Demo123@#`
- কোনো স্পেস নেই, ঠিক এই ক্যারেক্টারগুলো: D-e-m-o-1-2-3-@-#

#### সমস্যা ৩: পেজ লোড হচ্ছে না

**কারণ:** অ্যাডমিন প্যানেল চালু নেই

**সমাধান:**
```bash
cd clean-care-admin
npm run dev
```

#### সমস্যা ৪: "CORS Error"

**কারণ:** ব্যাকএন্ড এবং ফ্রন্টএন্ড এর মধ্যে কানেকশন সমস্যা

**সমাধান:**
1. উভয় সার্ভার বন্ধ করুন (Ctrl+C)
2. আবার চালু করুন:
   ```bash
   # টার্মিনাল ১
   cd server
   npm run dev
   
   # টার্মিনাল ২
   cd clean-care-admin
   npm run dev
   ```

---

## 🔍 ডিবাগিং টিপস

### API কানেকশন টেস্ট করুন

ব্রাউজারে এই URL খুলুন:
```
http://localhost:4000/api/admin/test
```

যদি কাজ করে, আপনার দেখা উচিত:
```json
{
  "message": "Admin API is working",
  "timestamp": "..."
}
```

### পাসওয়ার্ড আবার রিসেট করুন

যদি এখনও কাজ না করে, পাসওয়ার্ড আবার রিসেট করুন:

```bash
cd server
node check-and-fix-admin-password.js
```

---

## 📞 সাহায্য প্রয়োজন?

যদি এখনও সমস্যা হয়, নিচের তথ্য দিন:

1. **ব্রাউজার কনসোল এরর** (F12 → Console ট্যাব)
2. **সার্ভার লগ** (যেখানে `npm run dev` চালাচ্ছেন)
3. **কোন URL ব্যবহার করছেন**
4. **কোন পাসওয়ার্ড টাইপ করছেন**

---

## ✅ সফল লগইনের লক্ষণ

যদি লগইন সফল হয়:

1. পেজ রিডাইরেক্ট হবে `/admin/dashboard` এ
2. আপনার দেখা উচিত ড্যাশবোর্ড
3. সাইডবারে আপনার নাম দেখা যাবে
4. প্রোফাইল আইকন দেখা যাবে

তারপর আপনার প্রোফাইল আইকনে ক্লিক করে ProfileModal দেখতে পারবেন! 🎉

---

## 🎯 দ্রুত সমাধান চেকলিস্ট

- [ ] ব্যাকএন্ড সার্ভার চালু আছে (port 4000)
- [ ] ফ্রন্টএন্ড সার্ভার চালু আছে (port 5173)
- [ ] সঠিক URL ব্যবহার করছি: `http://localhost:5173/admin/login`
- [ ] সঠিক ইমেইল: `superadmin@demo.com`
- [ ] সঠিক পাসওয়ার্ড: `Demo123@#` (কোনো স্পেস নেই)
- [ ] ব্রাউজার কনসোলে কোনো এরর নেই
- [ ] API টেস্ট URL কাজ করছে: `http://localhost:4000/api/admin/test`
