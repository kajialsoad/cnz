# Test Environment Variables
# Use local MySQL database for performance testing

# IMPORTANT: You need to set up a local MySQL database for testing
# Option 1: Create a dedicated test database (recommended)
DATABASE_URL="mysql://root:your_password@localhost:3306/clean_care_test?sslmode=disable&connection_limit=1000&pool_timeout=60&connect_timeout=60"

# Option 2: Use your local development database (if you have one)
# DATABASE_URL="mysql://root:your_password@localhost:3306/clean_care_dev?sslmode=disable&connection_limit=1000&pool_timeout=60&connect_timeout=60"

# SETUP INSTRUCTIONS:
# 1. Install MySQL locally if not already installed
# 2. Create a test database: CREATE DATABASE clean_care_test;
# 3. Update the DATABASE_URL above with your MySQL root password
# 4. Run migrations: npx prisma migrate deploy
# 5. Run the performance tests: npm test -- bot-database-query.performance.test.ts --run
